<UserControl x:Class="AutoMacro.Views.VariableEditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             mc:Ignorable="d">

    <Grid Margin="16,12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Row 0: Title + Save -->
        <Grid Grid.Row="0" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock Text="变量表" FontSize="18" FontWeight="SemiBold" VerticalAlignment="Center"
                       Foreground="White" />
            <ui:Button Grid.Column="1" Icon="{ui:SymbolIcon Save24}" Content="保存"
                       Click="SaveCsv_Click" Appearance="Primary" />
        </Grid>

        <!-- Row 1: Row operations -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,8">
            <ui:Button Icon="{ui:SymbolIcon AddSquare24}" Content="添加行"
                       Click="AddRow_Click" Margin="0,0,4,0" />
            <ui:Button Icon="{ui:SymbolIcon Delete24}" Content="删除选中行"
                       Click="DeleteRow_Click" Margin="0,0,4,0" />
            <ui:Button Icon="{ui:SymbolIcon DismissCircle24}" Content="清空全部"
                       Appearance="Danger" Click="ClearAll_Click" />
        </StackPanel>

        <!-- Row 2: ListBox -->
        <ListBox Grid.Row="2"
                 x:Name="VarListBox"
                 ItemsSource="{Binding Items}"
                 SelectionMode="Extended"
                 HorizontalContentAlignment="Stretch"
                 BorderThickness="1"
                 BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <TextBox Text="{Binding Value, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                             BorderThickness="0"
                             Background="Transparent"
                             VerticalAlignment="Center"
                             Margin="4,2" />
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <!-- Row 3: Help -->
        <Border Grid.Row="3" Background="{DynamicResource ControlFillColorDefaultBrush}"
                CornerRadius="4" Padding="12,8" Margin="0,8,0,0">
            <TextBlock TextWrapping="Wrap" FontSize="12"
                       Foreground="{DynamicResource TextFillColorSecondaryBrush}">
                <Run FontWeight="SemiBold">使用说明：</Run><LineBreak />
                旧脚本仍可读取这里的数据；新版推荐使用“看到图片就点击”“等待图片出现”和窗口相对坐标。
            </TextBlock>
        </Border>
    </Grid>
</UserControl>
