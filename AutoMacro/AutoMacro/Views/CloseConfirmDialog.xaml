<Window x:Class="AutoMacro.Views.CloseConfirmDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="关闭确认"
        Width="340" Height="260" SizeToContent="Height"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        WindowStyle="SingleBorderWindow"
        Background="#2D2D2D">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="请选择关闭方式："
                   Foreground="White" FontSize="14" Margin="0,0,0,12" />

        <RadioButton Grid.Row="1" x:Name="RbExit"
                     Content="彻底退出软件"
                     Foreground="White" FontSize="13"
                     IsChecked="True" Margin="0,0,0,8" />

        <RadioButton Grid.Row="2" x:Name="RbMinimize"
                     Content="最小化到系统托盘"
                     Foreground="White" FontSize="13"
                     Margin="0,0,0,12" />

        <CheckBox Grid.Row="3" x:Name="CbRemember"
                  Content="勾选后不再提示"
                  Foreground="#AAAAAA" FontSize="12"
                  VerticalAlignment="Bottom"
                  Margin="0,0,0,8" />

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="取消" Click="Cancel_Click"
                    MinWidth="70" Margin="0,0,8,0" />
            <Button Content="确定" Click="Confirm_Click"
                    MinWidth="70" IsDefault="True" />
        </StackPanel>
    </Grid>
</Window>
